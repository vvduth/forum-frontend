query MyQuery {
  postsConnection {
    edges {
      node {
        author {
          bio
          name
          photo {
            url
          }
        }
        createdAt
        slug
        excerpt
        name
      }
    }
  }
}
